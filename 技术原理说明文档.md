# 解析几何题目生成系统 — 技术原理说明文档

**版本：** v1.0  
**日期：** 2026年5月29日  

---

## 目录

1. [项目背景与目标](#1-项目背景与目标)
2. [系统总体架构](#2-系统总体架构)
3. [数学原理基础](#3-数学原理基础)
4. [题目生成引擎设计](#4-题目生成引擎设计)
5. [精确配图渲染原理](#5-精确配图渲染原理)
6. [LaTeX 输出格式化](#6-latex-输出格式化)
7. [代码实现详解](#7-代码实现详解)
8. [测试与验证](#8-测试与验证)
9. [总结与展望](#9-总结与展望)

---

## 1. 项目背景与目标

### 1.1 项目背景

解析几何是高中数学的核心模块之一，涵盖椭圆、双曲线、抛物线和极坐标四大知识领域。传统出题方式依赖教师手工编写，存在以下问题：

- **效率低**：手工出一道解析几何题需要大量计算验证
- **配图不精确**：手工绘制的图形往往与数学参数不一致
- **题型单一**：受限于教师经验，题库覆盖面有限
- **难以批量生成**：无法快速生成大量变式题目

### 1.2 项目目标

本系统旨在实现以下目标：

| 目标 | 描述 | 实现方式 |
|------|------|----------|
| 自动生成 | 根据知识点和难度自动出题 | 参数化模板 + 随机采样 |
| 数学严谨 | 所有题目可解，解答完整 | 韦达定理 + 精确计算 |
| 精确配图 | 图形与参数严格一致 | Matplotlib参数方程渲染 |
| 格式规范 | LaTeX标准排版 | 模板化LaTeX生成 |

---

## 2. 系统总体架构

### 2.1 模块划分

系统由三个核心模块组成：

```
┌─────────────────────────────────────────────────┐
│              主程序 (main.py)                     │
│         协调各模块，提供统一入口                      │
└────────────┬──────────────┬──────────────┬───────┘
             │              │              │
    ┌────────▼────────┐ ┌───▼────────┐ ┌───▼────────────┐
    │ ProblemGenerator │ │ DiagramRenderer│ │ PPT Creator  │
    │   题目生成引擎    │ │  配图渲染引擎  │ │  演示文稿引擎  │
    └────────┬────────┘ └───┬────────┘ └───┬────────────┘
             │              │              │
    ┌────────▼────────┐ ┌───▼────────┐ ┌───▼────────────┐
    │   problem_generator.py  │ │ diagram_renderer.py │ │ create_ppt.py  │
    └─────────────────┘ └────────────┘ └─────────────────┘
```

### 2.2 数据流

```
用户指定(topic, difficulty)
        │
        ▼
  ProblemGenerator
  ├─ 参数采样 (a, b, c, e)
  ├─ 几何对象构造 (焦点, 顶点, 渐近线)
  ├─ 交点计算 (联立方程)
  └─ LaTeX生成
        │
        ▼
    Problem 对象
  { conic_params, points, lines,
    problem_latex, solution_latex }
        │
        ├──────────────────┐
        ▼                  ▼
  DiagramRenderer      PPT Creator
  ├─ 坐标系建立         ├─ 幻灯片布局
  ├─ 曲线绘制           ├─ 图片嵌入
  ├─ 点/线标注          └─ 导出 .pptx
  └─ 导出 .png
```

### 2.3 数据结构设计

#### 2.3.1 ConicParams（圆锥曲线参数）

```python
@dataclass
class ConicParams:
    center: Tuple[float, float]  # 中心点 (x₀, y₀)
    a: float      # 半长轴 (椭圆) / 半实轴 (双曲线) / 焦距参数 (抛物线)
    b: float      # 半短轴 (椭圆) / 半虚轴 (双曲线)
    c: float      # 半焦距
    e: float      # 离心率
    rotation: float  # 旋转角度 (弧度)
```

**参数关系：**

| 曲线类型 | c 与 a,b 关系 | 离心率 e |
|----------|---------------|----------|
| 椭圆 | c = √(a² − b²) | e = c/a < 1 |
| 双曲线 | c = √(a² + b²) | e = c/a > 1 |
| 抛物线 | p = 2a, c = p/2 | e = 1 |

#### 2.3.2 Point（关键点）

```python
@dataclass
class Point:
    x: float
    y: float
    label: str  # LaTeX 标签，如 "F_1", "A_1"
```

#### 2.3.3 Line（直线）

```python
@dataclass
class Line:
    a: float  # ax + by + c = 0
    b: float
    c: float
    label: str
```

**关键方法：**

- `distance_to_point(p)` — 计算点到直线距离
- `point_on_line(x)` — 给定 x 求 y
- `slope()` — 求斜率

#### 2.3.4 Problem（题目对象）

```python
@dataclass
class Problem:
    title: str           # 题目标题
    topic: str           # 知识点分类
    difficulty: int      # 难度等级 1/2/3
    problem_latex: str   # LaTeX 格式题干
    solution_latex: str  # LaTeX 格式完整解答
    conic_params: ConicParams
    points: List[Point]  # 关键点列表
    lines: List[Line]    # 关键直线列表
    conic_type: str      # "ellipse"/"hyperbola"/"parabola"/"polar"
    answer: str          # 最终答案 LaTeX
```

---

## 3. 数学原理基础

### 3.1 椭圆

#### 标准方程

$$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1 \quad (a > b > 0)$$

#### 基本参数

- **半长轴**：a（沿 x 轴）
- **半短轴**：b（沿 y 轴）
- **半焦距**：c = √(a² − b²)
- **离心率**：e = c/a ∈ (0, 1)
- **焦点**：F₁(−c, 0)，F₂(c, 0)
- **顶点**：A₁(−a, 0)，A₂(a, 0)，B₁(0, −b)，B₂(0, b)

#### 焦点弦长公式

过焦点 F₁(−c, 0) 且斜率为 k 的直线与椭圆的交点弦长：

$$|PQ| = \sqrt{1 + k^2} \cdot |x_1 - x_2|$$

其中 x₁, x₂ 是联立方程的两个根。

**联立过程：**

将 y = k(x + c) 代入 x²/a² + y²/b² = 1：

$$(b^2 + a^2k^2)x^2 + 2a^2ck^2x + a^2c^2k^2 - a^2b^2 = 0$$

由韦达定理：

$$x_1 + x_2 = \frac{-2a^2ck^2}{b^2 + a^2k^2}, \quad x_1 x_2 = \frac{a^2c^2k^2 - a^2b^2}{b^2 + a^2k^2}$$

#### 焦点三角形面积

在 △F₁PF₂ 中，由余弦定理：

$$4c^2 = |PF_1|^2 + |PF_2|^2 - 2|PF_1||PF_2|\cos\theta$$

结合 |PF₁| + |PF₂| = 2a，可得：

$$S_{\triangle F_1PF_2} = \frac{1}{2}|PF_1||PF_2|\sin\theta$$

### 3.2 双曲线

#### 标准方程

$$\frac{x^2}{a^2} - \frac{y^2}{b^2} = 1$$

#### 基本参数

- **半实轴**：a
- **半虚轴**：b
- **半焦距**：c = √(a² + b²)
- **离心率**：e = c/a > 1
- **渐近线**：y = ±(b/a)x

#### 渐近线方程推导

$$\frac{x^2}{a^2} - \frac{y^2}{b^2} = 0 \implies y = \pm\frac{b}{a}x$$

#### 焦点三角形面积

在 △F₁PF₂ 中，由余弦定理和双曲线定义：

$$||PF_1| - |PF_2|| = 2a$$

$$4c^2 = |PF_1|^2 + |PF_2|^2 - 2|PF_1||PF_2|\cos\theta$$

两式联立解得：

$$|PF_1| \cdot |PF_2| = \frac{2b^2}{1 - \cos\theta}$$

$$S_{\triangle F_1PF_2} = \frac{1}{2}|PF_1||PF_2|\sin\theta = \frac{b^2\sin\theta}{1 - \cos\theta}$$

### 3.3 抛物线

#### 标准方程（开口向右）

$$y^2 = 2px \quad (p > 0)$$

#### 基本参数

- **焦点**：F(p/2, 0)
- **准线**：x = −p/2
- **焦点参数**：p = 焦点到准线的距离

#### 焦点弦长公式

过焦点的弦，设倾斜角为 θ：

$$|PQ| = \frac{2p}{\sin^2\theta}$$

#### 焦点弦性质（竞赛题）

设 P(x₁, y₁)，Q(x₂, y₂)，则：

$$\frac{1}{|PF|} + \frac{1}{|QF|} = \frac{2}{p}$$

**证明要点：** 利用抛物线定义 |PF| = x₁ + p/2，|QF| = x₂ + p/2，以及焦点弦的韦达定理性质 x₁x₂ = p²/4。

### 3.4 极坐标

#### 圆的极坐标方程

$$\rho = 2r\cos\theta$$

表示圆心在 (r, 0)，半径为 r 的圆。

**推导：**

$$\rho = 2r\cos\theta \implies \rho^2 = 2r\rho\cos\theta$$

$$x^2 + y^2 = 2rx \implies (x-r)^2 + y^2 = r^2$$

#### 圆锥曲线的极坐标方程（以焦点为极点）

$$\rho = \frac{ep}{1 - e\cos\theta}$$

其中 e 为离心率，p 为焦点到相应准线的距离。

| e 值 | 曲线类型 |
|------|----------|
| e < 1 | 椭圆 |
| e = 1 | 抛物线 |
| e > 1 | 双曲线 |

---

## 4. 题目生成引擎设计

### 4.1 参数采样策略

系统采用**可控随机采样**策略：

```python
# 椭圆参数采样
a = np.random.choice([2, 3, 4, 5, 6])
b = np.random.choice([x for x in [1, 2, 3, 4, 5] if x < a])
c = np.sqrt(a**2 - b**2)
e = c / a
```

**采样约束：**

| 约束 | 说明 |
|------|------|
| a > b > 0 | 椭圆半长轴大于半短轴 |
| b > 0 | 双曲线虚轴为正 |
| p > 0 | 抛物线参数为正 |
| 斜率 k ≠ 0 | 避免退化情况 |

### 4.2 可解性保证

每道题目的生成都保证：

1. **参数合理性**：所有几何参数在有效范围内
2. **交点存在性**：判别式 Δ ≥ 0 保证实交点存在
3. **解答完整性**：自动推导并输出完整解题过程
4. **数值精确性**：使用浮点运算，保留4位有效数字

### 4.3 难度梯度设计

| 难度 | 知识点 | 考察能力 | 题型示例 |
|------|--------|----------|----------|
| ★☆☆ 基础 | 标准方程、基本参数 | 概念记忆、公式应用 | 求椭圆方程、焦点坐标 |
| ★★☆ 进阶 | 弦长、面积、位置关系 | 综合计算、方程联立 | 焦点弦长、三角形面积 |
| ★★★ 竞赛 | 综合证明、最值问题 | 逻辑推理、数学证明 | 焦点三角形性质证明 |

### 4.4 LaTeX 模板化生成

题干和解答使用 Python f-string 模板化生成：

```python
problem_latex = (
    f"已知椭圆 $\\frac{{x^2}}{{{a**2}}} + \\frac{{y^2}}{{{b**2}}} = 1$ "
    f"的左焦点为 $F_1$，过 $F_1$ 且斜率为 ${k}$ 的直线 $l$ "
    f"与椭圆交于 $P$、$Q$ 两点。\n\n"
    f"(1) 求弦 $PQ$ 的长；\n\n"
    f"(2) 求 $\\triangle PF_1Q$ 的面积。"
)
```

**LaTeX 转义规则：**

- `{{` → `{`（花括号转义）
- `}}` → `}`
- `$...$` → 行内数学公式
- `$$...$$` → 块级数学公式

---

## 5. 精确配图渲染原理

### 5.1 坐标系建立

```python
def _setup_figure(self, x_range, y_range):
    # 1. 设置坐标轴范围（含15%边距）
    ax.set_xlim(x_range[0] - margin, x_range[1] + margin)
    ax.set_ylim(y_range[0] - margin, y_range[1] + margin)

    # 2. 绘制网格
    ax.grid(True, linestyle='-', alpha=0.3)

    # 3. 绘制坐标轴（带箭头）
    ax.axhline(y=0, linewidth=1.2)
    ax.axvline(x=0, linewidth=1.2)
    ax.annotate('', xy=(x_end, 0), arrowprops={...})

    # 4. 等比例显示
    ax.set_aspect('equal')
```

### 5.2 圆锥曲线绘制

#### 椭圆（参数方程）

$$x = a\cos\theta, \quad y = b\sin\theta \quad \theta \in [0, 2\pi)$$

```python
theta = np.linspace(0, 2 * np.pi, 1000)
x = center_x + a * np.cos(theta)
y = center_y + b * np.sin(theta)
ax.plot(x, y, color=curve_color, linewidth=2)
```

#### 双曲线（显式方程）

$$y = \pm b\sqrt{\frac{x^2}{a^2} - 1}$$

```python
x_right = np.linspace(a, x_max, 500)
y_pos = b * np.sqrt((x_right/a)**2 - 1)
y_neg = -y_pos
ax.plot(x_right, y_pos, color=curve_color)
ax.plot(x_right, y_neg, color=curve_color)
```

#### 抛物线（显式方程）

$$y^2 = 2px \implies x = \frac{y^2}{2p}$$

```python
y = np.linspace(y_min, y_max, 500)
x = y**2 / (2*p)
ax.plot(x, y, color=curve_color)
```

#### 极坐标圆（参数方程）

$$\rho = 2r\cos\theta \implies x = 2r\cos^2\theta, \quad y = 2r\cos\theta\sin\theta$$

```python
theta = np.linspace(-np.pi/2, np.pi/2, 500)
rho = 2 * r * np.cos(theta)
x = rho * np.cos(theta)
y = rho * np.sin(theta)
```

### 5.3 采样点数选择

| 元素 | 采样点数 | 理由 |
|------|----------|------|
| 圆锥曲线 | 1000 | 保证光滑度，避免锯齿 |
| 直线 | 500 | 两端延伸覆盖全范围 |
| 渐近线 | 200 | 线性关系，无需过多采样 |

### 5.4 颜色编码系统

| 颜色 | HEX | 用途 |
|------|-----|------|
| 蓝色 | #2E86AB | 圆锥曲线（椭圆、双曲线、抛物线） |
| 红色 | #E8451E | 直线、弦 |
| 紫色 | #9B59B6 | 渐近线（虚线） |
| 绿色 | #27AE60 | 准线（点划线） |
| 金色 | #FFD700 | 关键点填充 |
| 深灰 | #333333 | 标签文字 |

### 5.5 点标注系统

```python
def _plot_point(self, ax, point, color=None, fill_color=None,
                size=50, label_offset=(0.15, 0.15)):
    # 1. 绘制实心圆点
    ax.scatter(point.x, point.y, c=fill_color, s=size,
               edgecolors=color, linewidths=1.5, zorder=10)

    # 2. 添加 LaTeX 标签
    if point.label:
        ax.text(point.x + offset[0], point.y + offset[1],
                f'${point.label}$', fontsize=11, color=label_color)
```

**z-order 层次：**

| 层级 | z-order | 内容 |
|------|---------|------|
| 1 | 2 | 坐标轴、网格 |
| 2 | 3 | 曲线填充 |
| 3 | 4 | 曲线轮廓 |
| 4 | 5 | 直线 |
| 5 | 10 | 点标注 |
| 6 | 11 | 标签文字 |

---

## 6. LaTeX 输出格式化

### 6.1 题干模板

```latex
已知椭圆 $C$ 的中心在原点，焦点在 $x$ 轴上，
长轴长为 ${2a}$，短轴长为 ${2b}$。

(1) 求椭圆 $C$ 的标准方程；

(2) 求椭圆 $C$ 的焦点坐标和离心率。
```

### 6.2 解答模板

```latex
**解：**

(1) 由题意知 $a = {a}$，$b = {b}$，
$c = \sqrt{a^2 - b^2} = \sqrt{{a^2} - {b^2}} = {c}$。

椭圆的标准方程为：
$$\frac{x^2}{{a^2}} + \frac{y^2}{{b^2}} = 1$$

(2) 焦点坐标为 $F_1({-c}, 0)$，$F_2({c}, 0)$。

离心率 $e = \frac{c}{a} = {e}$。
```

### 6.3 韦达定理格式化

```latex
由韦达定理：$x_1 + x_2 = \frac{-B}{A}$，$x_1 x_2 = \frac{C}{A}$

$|PQ| = \sqrt{1 + k^2} \cdot |x_1 - x_2| = \sqrt{1 + k^2} \cdot \sqrt{(x_1+x_2)^2 - 4x_1x_2}$
```

---

## 7. 代码实现详解

### 7.1 项目文件结构

```
analytic_geometry_generator/
├── main.py                    # 主程序入口
├── problem_generator.py       # 题目生成引擎
├── diagram_renderer.py        # 配图渲染引擎
├── create_ppt.py              # PPT演示文稿创建
├── README.md                  # 项目说明
├── 技术原理说明文档.md          # 本文档
├── 答辩PPT.pptx               # 答辩演示文稿
├── DESIGN.md                  # 设计规范参考
└── output/                    # 输出目录
    ├── 椭圆_difficulty1.png
    ├── 椭圆_difficulty2.png
    ├── 椭圆_difficulty3.png
    ├── 双曲线_difficulty1.png
    ├── 双曲线_difficulty2.png
    ├── 双曲线_difficulty3.png
    ├── 抛物线_difficulty1.png
    ├── 抛物线_difficulty2.png
    ├── 抛物线_difficulty3.png
    ├── 极坐标_difficulty1.png
    ├── 极坐标_difficulty2.png
    ├── 极坐标_difficulty3.png
    ├── 解析几何题目生成系统.pptx
    └── 技术原理说明文档.md
```

### 7.2 核心类设计

#### ProblemGenerator

```python
class ProblemGenerator:
    def __init__(self, seed: int = None):
        # 初始化随机种子，保证可复现

    def generate(self, topic: str, difficulty: int) -> Problem:
        # 统一入口，路由到具体生成器

    def generate_ellipse_basic(self) -> Problem:
        # 椭圆基础题

    def generate_ellipse_intermediate(self) -> Problem:
        # 椭圆进阶题（焦点弦）

    def generate_ellipse_competition(self) -> Problem:
        # 椭圆竞赛题（焦点三角形）

    # ... 双曲线、抛物线、极坐标类似
```

#### DiagramRenderer

```python
class DiagramRenderer:
    def __init__(self, figsize=(8, 6), dpi=150):
        # 初始化画布参数

    def render(self, problem: Problem, output_path: str) -> str:
        # 渲染入口

    def _setup_figure(self, x_range, y_range):
        # 建立坐标系

    def _plot_ellipse(self, ax, params, ...):
        # 绘制椭圆

    def _plot_hyperbola(self, ax, params, ...):
        # 绘制双曲线

    def _plot_parabola(self, ax, params, ...):
        # 绘制抛物线

    def _plot_point(self, ax, point, ...):
        # 绘制点

    def _plot_line(self, ax, line, ...):
        # 绘制直线
```

### 7.3 关键算法实现

#### 焦点弦交点计算

```python
# 联立直线 y = k(x + c) 与椭圆 x²/a² + y²/b² = 1
A_coeff = b**2 + a**2 * k**2
B_coeff = 2 * a**2 * c * k**2
C_coeff = a**2 * c**2 * k**2 - a**2 * b**2

# 求根公式
discriminant = B_coeff**2 - 4 * A_coeff * C_coeff
x1 = (-B_coeff + np.sqrt(discriminant)) / (2 * A_coeff)
x2 = (-B_coeff - np.sqrt(discriminant)) / (2 * A_coeff)

# 代入直线方程求 y
y1 = k * (x1 + c)
y2 = k * (x2 + c)
```

#### 点到直线距离

```python
def distance_to_point(self, p: Point) -> float:
    """点 (x₀, y₀) 到直线 ax + by + c = 0 的距离"""
    return abs(self.a * p.x + self.b * p.y + self.c) / \
           np.sqrt(self.a**2 + self.b**2)
```

---

## 8. 测试与验证

### 8.1 参数一致性验证

| 验证项 | 方法 | 预期结果 |
|--------|------|----------|
| 椭圆参数 | 检查 a > b > 0 | ✓ |
| 焦距关系 | c = √(a² − b²) | ✓ |
| 离心率 | e = c/a ∈ (0,1) | ✓ |
| 交点判别式 | Δ ≥ 0 | ✓ |
| 韦达定理 | x₁+x₂, x₁x₂ 正确 | ✓ |

### 8.2 配图精度验证

- 所有曲线使用 1000 个采样点，视觉光滑无锯齿
- 关键点坐标由精确计算得到，非估算
- 坐标轴等比例显示，几何关系视觉正确
- 点标签位置不重叠

### 8.3 LaTeX 格式验证

- 所有数学公式正确嵌套
- 花括号转义正确
- 符号（±, ×, ÷）正确显示
- 答案格式完整

---

## 9. 总结与展望

### 9.1 技术总结

本系统实现了以下技术突破：

1. **参数化题目生成**：通过随机采样 + 约束条件，自动生成多样化题目
2. **数学可解性保证**：所有中间量精确计算，解答完整可靠
3. **精确配图渲染**：Matplotlib 参数方程绘制，图形与参数严格一致
4. **LaTeX 标准输出**：规范的数学排版，便于教学使用

### 9.2 扩展方向

| 方向 | 描述 | 优先级 |
|------|------|--------|
| 更多知识点 | 圆、参数方程、直线方程 | 高 |
| TikZ 输出 | LaTeX TikZ 绘图代码导出 | 高 |
| Web 界面 | 在线题目生成平台 | 中 |
| 题目数据库 | 生成题目持久化存储 | 中 |
| 智能组卷 | 知识点覆盖率自动组卷 | 低 |
| 自定义难度 | 更细粒度的难度控制 | 低 |

### 9.3 技术栈

- **语言**：Python 3.12
- **数值计算**：NumPy
- **绘图**：Matplotlib
- **PPT 生成**：python-pptx
- **数学排版**：LaTeX

---

**文档结束**
